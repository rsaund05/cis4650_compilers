/* A program to compute the factorial value of an input */

void main(void fuck) {
  int x; int fac; int x;
  x = input(fac);
  fac = 1;
  
  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  } 

  output();
}
