/* This program is meant to demonstrate error handling for an invalid statement, and 
   an invalid return statement. */

void main(void)
{
    int x;
    int y
    x + x = x;
    y = x;
    y =  + x;
    test(one, two);
    return
}